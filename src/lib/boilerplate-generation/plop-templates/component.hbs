import React from 'react';
{{#if hooks}}import { {{hookImports hooks}} } from '../hooks';{{/if}}
{{#if store}}import { {{storeImport store}} } from '../store';{{/if}}
{{#if types}}import type { {{firstType types}} } from '../types';{{/if}}

interface {{properCase name}}Props {
  // Add your props here
}

export const {{properCase name}}: React.FC<{{properCase name}}Props> = ({}) => {
  {{#if hooks}}// Hooks usage{{/if}}
  {{#each hooks}}
  const {{camelCase this}} = use{{properCase this}}();
  {{/each}}
  
  {{#if store}}// Store usage{{/if}}
  {{#if store}}
  const { state, actions } = {{store}}();
  {{/if}}
  
  return (
    <div className="{{dashCase name}}-component" data-testid="{{dashCase name}}-component">
      {/* Your component JSX */}
    </div>
  );
};